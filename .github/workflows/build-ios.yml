name: Build IPTVAK for iOS

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer || sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
    
    - name: Show Xcode version
      run: xcodebuild -version
    
    - name: Build for iOS Device
      run: |
        xcodebuild clean build \
          -project IPTVAK.xcodeproj \
          -scheme IPTVAK \
          -sdk iphoneos \
          -configuration Debug \
          -derivedDataPath build \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGN_IDENTITY="" \
          DEVELOPMENT_TEAM="" \
          SDKROOT=iphoneos \
          TARGETED_DEVICE_FAMILY="1,2" \
          IPHONEOS_DEPLOYMENT_TARGET=16.0 \
          SUPPORTS_MACCATALYST=NO \
          ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon
    
    - name: Find and package app
      run: |
        echo "=== Looking for .app files ==="
        find build -name "*.app" -type d
        
        # Copy app to known location
        mkdir -p output
        find build -name "IPTVAK.app" -type d -exec cp -r {} output/ \;
        
        # Create IPA for sideloading
        cd output
        mkdir -p Payload
        cp -r IPTVAK.app Payload/
        zip -r IPTVAK-iOS.ipa Payload
        ls -la
    
    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: IPTVAK-iOS-App
        path: output/IPTVAK-iOS.ipa
        if-no-files-found: error
